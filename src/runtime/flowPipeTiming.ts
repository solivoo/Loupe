/**
 * Duración del recorrido de la bolita en el trazo (PacketEdge `animateMotion`).
 * La línea en el panel Consola aparece después del mismo tiempo en `StepController.logConsole`.
 */
export const FLOW_PIPE_ANIMATION_SEC = 1.12

export const FLOW_PIPE_TO_CONSOLE_MS = Math.round(FLOW_PIPE_ANIMATION_SEC * 1000)
